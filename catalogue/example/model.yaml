id: example/checkpoint-text-to-image
profile: example
operation: image_generation
workflow: workflow.json
workflow_sha256: 9a3a03db94ef637ff3efe74aeff4871a46559e4fa0033d0ef5dec70429581e24
owned_by: comfy-control
aliases:
  - example
input_map:
  prompt: { node: "1", input: text }
  width: { node: "2", input: width }
  height: { node: "2", input: height }
  seed: { node: "3", input: seed }
  steps: { node: "3", input: steps }
output: { node: "4", type: image }
defaults:
  width: 1024
  height: 1024
  seed: 0
  steps: 20
required_files:
  - checkpoints/example.safetensors
