[env]
# Gateway
API_KEY = "REPLACE_ME"
COMFY_UI_PASSWORD = "REPLACE_ME"
COMFY_UI_USERNAME = "REPLACE_ME"

# Model downloads (gated repos require HF_TOKEN)
HF_TOKEN = "REPLACE_ME"
CIVITAI_TOKEN = "REPLACE_ME"

# S3-compatible output storage (optional)
S3_ENDPOINT_URL = "REPLACE_ME"
S3_BUCKET = "REPLACE_ME"
S3_ACCESS_KEY_ID = "REPLACE_ME"
S3_SECRET_ACCESS_KEY = "REPLACE_ME"
S3_REGION = "us-east-1"
S3_PREFIX = "outputs"
S3_PUBLIC_BASE_URL = ""
S3_URL_EXPIRES = "3600"

# Registry authentication for `mise run deploy` (GHCR)
GHCR_TOKEN = "REPLACE_ME"
GHCR_USER = "maxexcloo"
